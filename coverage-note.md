# บันทึกผลการวัด Test Coverage และตอบคำถาม (Lab 5 ขั้นที่ 5)
**รายวิชา**: วิศวกรรมซอฟต์แวร์ — Team 13

## สรุปผลการรัน Coverage (จาก `pytest --cov=src --cov-report=term-missing`)
* **Coverage รวมของระบบ Domain Logic (`src/`)**: **100%**
* **จำนวนชุดทดสอบทั้งหมด**: 17 Automated Tests ผ่านทั้งหมด 100%

---

## ตอบคำถามท้ายการวัดผล 3 ข้อ (Lab 5 ขั้นที่ 5)

### 1. บรรทัดไหนที่ test ยังไม่เคยรันผ่านเลย และการที่มันหลุดไป เสี่ยงอย่างไร?
> **คำตอบ**:  
> ในการทดสอบรอบแรกก่อนทำ TDD พบว่าในส่วนของ `src/service.py` เมธอด `low_stock_items` และการดักจับข้อผิดพลาด `quantity <= 0` และ `is_active == False` ยังไม่เคยถูกเรียกใช้งาน (Uncovered Lines)  
> **ความเสี่ยง**: หากโค้ดส่วนนี้หลุดเข้าสู่สภาพแวดล้อม Production ผู้ใช้อาจสั่งซื้อสินค้าเป็น 0 ชิ้น หรือกรอกเลขติดลบเพื่อแฮกเพิ่มสต็อก หรือสั่งซื้อสินค้าที่ยกเลิกการจำหน่ายไปแล้วได้ ทำให้เกิดความเสียหายทางการเงินและสต็อกสินค้าในคลังผิดพลาด

### 2. ทำไม Coverage 100% ถึงยังไม่ได้แปลว่า Test ดี? ยกตัวอย่างจากโค้ดตัวเองหนึ่งกรณี
> **คำตอบ**:  
> Code Coverage วัดเพียงแค่ว่า **"มีบรรทัดโค้ดใดบ้างที่ถูกรันผ่าน"** แต่ไม่ได้การันตีว่า **"Assertion ตรวจสอบเงื่อนไขถูกต้องและครอบคลุมความหมายทางธุรกิจหรือไม่"**  
> **ตัวอย่างจากโค้ดทีม**: ในตอนแรกที่ให้ AI เขียนเทส AI สั่งขายสินค้าดิจิทัล 1 ชิ้น โค้ดรันผ่านทุกบรรทัด (Coverage 100%) แต่ AI ไม่ได้เขียน `assert remaining_stock == "Unlimited"` และไม่ได้ทดสอบกรณีที่สถานะยังเป็น `Pending` ว่าลิงก์ดาวน์โหลดต้องถูกปิดล็อก ทำให้ระบบดูเหมือนผ่านการทดสอบ 100% แต่ยังมีช่องโหว่ความปลอดภัยทางธุรกิจร้ายแรงแฝงอยู่

### 3. ถ้ามีเวลาเขียน Test เพิ่มได้แค่ 3 ข้อ จะเลือกเขียนข้อไหนก่อน เพราะอะไร?
> **คำตอบ**:  
> ทีมเลือกเขียน 3 ข้อนี้ก่อนตามลำดับความสำคัญของความเสี่ยง (Risk-Based Testing):
> 1. **`test_physical_product_cannot_sell_beyond_stock` (ห้ามขายเกินสต็อก)**: เพราะเป็น Core Invariant ของระบบคลังสินค้า หากขายเกินสต็อกจะทำให้เกิดปัญหาสินค้าไม่พอส่งและสูญเสียความน่าเชื่อถือ
> 2. **`test_digital_product_download_locked_when_pending` (ล็อกไฟล์ดาวน์โหลดหากยังไม่จ่ายเงิน)**: เพราะเป็นประเด็นความปลอดภัย (Security Guardrail) หากลูกค้าที่ยังไม่ชำระเงินสามารถดาวน์โหลดหนังสือดิจิทัลได้ บริษัทจะสูญเสียรายได้ทันที
> 3. **`test_sell_negative_quantity_raises_value_error` (ป้องกันค่าลบ)**: เพราะเป็นการป้องกันช่องโหว่เชิงตรรกะ (Logic Exploit) ที่อาจทำให้ยอดสินค้าคงคลังถูกบวกเพิ่มแทนที่จะหักออก
