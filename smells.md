# รายการ Code Smells ที่พบในโค้ดเดิม (pricing_legacy.py)
**รายวิชา**: วิศวกรรมซอฟต์แวร์ (Lab 5 ขั้นที่ 6) — Team 13

จากการวิเคราะห์โค้ด `pricing_legacy.py` ด้วยการรีวิวแบบละเอียด พบจุดที่อ่านยากและมีความเสี่ยง 6 ประการ ดังนี้:

| # | ชื่อ Code Smell | จุดที่พบในโค้ด | อธิบายปัญหาและผลกระทบ |
|---|---|---|---|
| 1 | **Long Method & Low Cohesion** | ฟังก์ชัน `calc()` | ฟังก์ชันเดียวรับผิดชอบทุกอย่าง ทั้งตรวจข้อมูล, คิดส่วนลดสมาชิก, คิดคูปอง, คิดส่วนลดวันหยุด และปัดเศษ ทำให้แก้ไขยาก |
| 2 | **Magic Numbers** | ตัวเลข `0.15`, `0.10`, `0.05`, `1000`, `500` | ตัวเลขเงื่อนไขและเปอร์เซ็นต์ส่วนลดถูกใส่ตรงๆ โดยไม่มีชื่อตัวแปรบอกความหมาย หากฝ่ายการตลาดต้องการปรับเกณฑ์ ต้องไล่แก้ตัวเลขทีละบรรทัด |
| 3 | **Cryptic Variable Names (Poor Naming)** | `p`, `q`, `tot`, `d`, `c_type`, `is_wknd` | การตั้งชื่อตัวแปรเป็นตัวย่อสั้นๆ สื่อความหมายไม่ชัดเจน ทำให้ผู้พัฒนาคนอื่นต้องใช้เวลาทำความเข้าใจนาน |
| 4 | **Deeply Nested Conditionals (Arrow Anti-Pattern)** | บล็อก `if c_type == "VIP": if tot > 1000:` | มีการซ้อนเงื่อนไขหลายชั้น ทำให้ Flow การทำงานซับซ้อน (Cyclomatic Complexity สูง) เสี่ยงต่อการเกิด Bug เมื่อเพิ่มเงื่อนไขใหม่ |
| 5 | **Missing Type Hints & Docstrings** | คำประกาศฟังก์ชัน `def calc(...)` | ไม่มี Type Hinting และ Docstring อธิบายชนิดพารามิเตอร์และประเภทค่าส่งคืน ทำให้ IDE ไม่สามารถช่วยตรวจจับข้อผิดพลาดก่อนรันได้ |
| 6 | **Violation of Single Responsibility Principle (SRP)** | รวม Rule ธุรกิจ 3 กฎไว้ในฟังก์ชันเดียว | กฎการคำนวณราคาถูกผูกรวมกัน หากต้องการนำเฉพาะส่วนลดคูปองไปใช้ในหน้าสรุปยอดตะกร้าสินค้า จะไม่สามารถดึงออกไปใช้ซ้ำได้ |
